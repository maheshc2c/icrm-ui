export interface OpportunityFunnelHistory {
  oppId: number;
  searchDate: string;
  leadDetails: string;
  product: string;
  qty: number;
  valueLakhs: number;
  lifeTimeDays: number;
  stage: string;
  currentStage: string;
}